# RedTeam 검토 (악마의 변호인)

## 목표 A: 시공실적 자동등록

| # | 도전 | 심각도 | 완화책 / STOP |
|---|---|---|---|
| R1 | **[가정 깨기] "영상과 모델이 같은 건물"** — `docs/scan2bim-progress.md:56`이 정반대를 기록("현재 영상은 다른 건물이라 정합 대상 없음", 2026-06-16, 해당 영상 업로드 다음날). 영상엔 "iaan LOUNGE" 사인·목재 마감 기둥·VR/AR 포스터가 있는 사무실이고, 모델은 `G7F_FAB_*`(FAB 7층) | 🔴 | **STOP** — 사용자가 A-1을 확인하기 전까지 모든 정합 작업 중단. 다른 건물이면 이번 세션 전체(축분리 스케일·place_rigid·이방성 결론)가 물리적 무의미 |
| R2 | **[순환논증] 이번 세션의 "검증"이 자기충족** — s_h=3.644를 "recon 0.878 ↔ 모델 3.2m"로 역산해 놓고, 그 값으로 만든 결과가 "수동 분석과 소수점까지 일치"한다고 판정. 같은 입력을 두 번 쓴 것이지 독립 검증이 아님 | 🔴 | **STOP** — "일치"를 근거로 인용 금지. 독립 검증은 *모델과 무관한* 실측(줄자로 잰 복도폭, VIO pose, 영상 내 알려진 치수)으로만 가능 |
| R3 | **[가정 깨기] 이방성(수평 2~3배 압축)** — R2에 의존. 유일한 모델-독립 앵커 s_cam도 "눈높이 1.5m" 가정 [코드:scan2bim/metric_scale.py:29]. 등방 + 실제 복도 1.44m + 느린 촬영보행(0.28m/s)도 데이터를 동등하게 설명함 | 🔴 | **STOP** — 이방성을 확정 사실로 문서화한 메모리/PRD를 "미확정 가설"로 강등. 판별엔 기준길이 1개 실측 필요 |
| R4 | **[숨은 비용] 상용 대체재 존재** — Doxel/Buildots/OpenSpace가 동일 기능을 이미 제공하고, 우리가 막힌 지점을 360°캠으로 우회 | 🔴 | 자체 구현 지속의 **차별화 근거를 먼저 문서화**하라(사내 데이터 정책? `.dtdx` 네이티브? 비용?). 없으면 DROP 후보 |
| R5 | **[가정 깨기] "카메라가 닿음 = 시공됨"** — 카메라에 보였다는 것은 그 위치에 *무언가* 있다는 뜻이지 *설계대로 시공됐다*는 뜻이 아님. 오시공·타 계통 오인·가설재를 실적으로 오등록 가능 | 🟡 | 판정 계층화: 보임(coverage) → 존재(기하 일치 0.6 게이트) → 시공확정(**사람 검수 유지**). PRD 불변식 "red 정합에서 자동 실적 확정 금지" 준수 [문서:docs/scan2bim-auto-progress-prd.md:3] |
| R6 | **[실패 모드] 미촬영 = 미시공 오판** — 촬영 누락 구역이 "미시공"으로 등록되면 실적이 거짓으로 후퇴 | 🟡 | `not_observed`와 `absent`를 반드시 분리(현 coverage는 이미 분리 — `status_counts`에 `not_observed`/`out_of_scope` 존재). 실적 원장에 "미관측"을 3번째 상태로 유지 |
| R7 | **[실패 모드] LOD 불일치** — 모델은 구조·MEP만, 현장은 마감 포함. 마감 공정 실적은 모델에 대상 객체 자체가 없음 | 🟡 | 자동화 범위를 **모델에 존재하는 분야(구조·MEP)로 한정** 명시. 마감은 비대상 선언 |
| R8 | **[범위 크리프] 텍스트/학습 방향** — "특징점을 못 찾으니 학습시키자"는 자연스러운 다음 수지만, 실측상 원인이 아님(합성 0.003m 정확, 실패는 앵커 밀도/대응) | 🟡 | 학습 트랙 착수 금지. 먼저 기하 대응(직경+connector) 소진 |
| R9 | **[되돌리기] 롤백 가능한가** | 🟢 | 가능 — 모든 작업이 git worktree 격리 + 사이클 커밋. `feat/auto-scale-fusion`은 언제든 `d0bcb14`로 되돌림 |

## 목표 B: 반복객체 앵커

| # | 도전 | 심각도 | 완화책 / STOP |
|---|---|---|---|
| R10 | **[가정 깨기] 직경+토폴로지가 반복을 깰 것이다** | 🟡 | 미검증 가설. 동일 직경·동일 계통 배관이 평행 다수면 시그니처도 중복될 수 있음 → 착수 전 **모델 데이터로 먼저 확인**: connector 그래프에서 인스턴스별 시그니처의 **고유성 비율**을 측정(코드 없이 분석만 가능). 고유성 낮으면 DROP |
| R11 | **[숨은 비용] 시그니처가 유일해도 현장 점군에서 직경 추정이 부정확** | 🟡 | 점군 단면 직경 추정 오차 실측 먼저(스파이크) |
| R12 | **[범위 크리프] 완전 무인 전역정합 추구** | 🔴 | **이미 2회 CEILING.** 불변식대로 "1힌트(시작점 1탭) + 자동"을 목표로 고정. 무인 전역정합 재시도 금지 |
