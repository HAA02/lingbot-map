# 방법론 선택

## 목표 A: 시공실적 자동등록

- **작업 성격**: 기술 불확실성 최대 + 토대 전제(A-1 건물 동일성) 미확인
- **추천 방법론**: **Spike(시간상자 실험) → 재판단**. TDD 금지(아직 무엇을 테스트할지 확정 불가)
- **산출할 아티팩트**:
  1. **Spike-0 (30분, 사람만 가능)**: upload_1781521406685이 Gasan_7F를 촬영한 것인지 사용자 확인. *이것 없이는 아래 전부 무의미.*
  2. **Spike-1**: Gasan 7F 실촬영 영상 1건 확보 → 기존 엔진(FR-2.1 registration)으로 정합 시도 → 성공/실패로 재판단
  3. **Spike-2**: ARKit/ARCore VIO pose를 모바일 업로드 페이로드에 실을 수 있는지 확인(단안 스케일 모호성 소스 제거 가능 여부)
- **AutoPM 신호**: **루프 투입 금지**. Spike-0 결과 없이는 어떤 코드 작업도 검증 불가

## 목표 B: 반복객체 앵커 기준점

- **작업 성격**: 도메인 로직 명확(직경+토폴로지 시그니처), 단 목표 A에 종속
- **추천 방법론**: **YAGNI + TDD** — A가 GO된 뒤 착수. 착수 시엔 `connector`(74~609개) 디코드 → 직경·토폴로지 시그니처 매칭을 TDD로
- **산출할 아티팩트**: 테스트 먼저(합성 반복배관에서 시그니처가 인스턴스를 유일 확정하는지)
- **AutoPM 신호**: **HOLD** — A의 Spike-0/1 통과 후 TDD 게이트 적용

## 이번 세션 산출물(place_rigid 등)의 처우

- **작업 성격**: 이미 구현·병합됨. 단 A-1 미확인으로 **물리적 타당성 미검증**
- **추천**: 코드는 유지(회귀 0, 게이트 통과)하되 **문서에 "건물 동일성 미확인" 경고 명시**. A-1이 거짓으로 판명되면 `--horizontal-scale-override 3.644`와 `check_coplay_geometry`의 수용 범위(turn-z-max 4 / end-x -8,4)는 **다른 건물 기준으로 튜닝된 값**이므로 폐기 대상
- **AutoPM 신호**: 신규 루프 금지, 문서화만
