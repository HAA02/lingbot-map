# Why 게이트 결과

- 실행: 2026-07-17 (`/PreFlight`)
- 검증 대상: 현장 영상↔BIM 매핑 기반 시공실적 자동등록

---

## 목표 A: 시공실적 자동등록 (카메라가 닿은 객체 = 시공됨 자동 판정)

| 항목 | 내용 | 출처 |
|---|---|---|
| 문제 정의 | 현장 시공 실적을 사람이 도면 대조로 수기 등록 중 → 영상 촬영만으로 자동 등록 | [문서:docs/scan2bim-auto-progress-prd.md:0. 한 줄 요약] |
| 진짜 문제? | 일일 실적등록은 반복·고빈도 업무. PRD가 "현장 작업자는 촬영만, 시스템이 설계 대조·실적 등록" 비전 명시 | [문서:docs/scan2bim-auto-progress-prd.md:1. 배경/목표] |
| 왜 지금 | 재구성·객체모델·coverage·뷰어 인프라가 이미 준비됨("정합 코어만 교체") | [문서:docs/scan2bim-auto-progress-prd.md:2. 핵심 통찰] |
| 안 만들면 | 수기 실적등록 지속 (구체적 비용/시간 수치는 **미기재**) | 🚨 [출처없음] — 정량 근거 없음 |
| 성공 기준 | 정합 성공률 ≥90%, 색-계통 매칭 ≥90%, 실적 일치율 ≥95% | [문서:docs/scan2bim-auto-progress-prd.md:9. 성공 지표] |

### 🚨 환각 후보 / 미검증 전제

| # | 항목 | 상태 |
|---|---|---|
| A-1 | **"upload_1781521406685 영상과 Gasan_7F 모델이 같은 건물"** | 🚨 **반대 근거 존재** — `docs/scan2bim-progress.md:56`(2026-06-16 작성): *"정확 위치(XY·yaw): 매칭되는 현장 영상(Gasan 7F) 필요 — **현재 영상은 다른 건물이라 정합 대상 없음**"*. 이 영상은 Jun 15 20:03 업로드로 문서 작성 시점의 "현재 영상"에 해당 [코드:realtime/_uploads/upload_1781521406685.mp4 mtime] |
| A-2 | **"이번 세션의 스케일 검증(3.644/20.51m/0.581m/s)이 정확도를 입증"** | 🚨 **순환논증** — s_h=3.644는 recon 복도폭 0.878을 **모델의** 복도 3.2m에 맞춰 역산한 값. A-1이 거짓이면 3.2m은 다른 건물 치수이므로 이 "일치"는 아무것도 증명 못 함 |
| A-3 | **"이방성(수평 2~3배 압축) 확정"** | 🚨 **A-2에 의존** — 이방성 주장의 근거가 "복도폭이 s_v로는 안 맞고 s_h로 맞는다"인데, 그 복도폭 자체가 A-1 전제. A-1이 거짓이면 recon은 등방이고 실제 복도가 1.44m(=0.878×s_cam 1.639)일 수 있음. 유일한 모델-독립 앵커는 s_cam이나 이것도 "눈높이 1.5m" **가정** [코드:scan2bim/metric_scale.py:29 assumed_height=1.5] |
| A-4 | 수기 실적등록의 현재 비용(시간/인건비/오류율) | 🚨 [출처없음] — ROI 판단 근거 부재 |
| A-5 | 실적 판정 정확도 검증용 **정답 라벨(ground truth)** 존재 여부 | 🚨 [출처없음] — 성공지표 "실적 일치율 ≥95%(수동 라벨 대비)"의 수동 라벨이 실재하는지 미확인 |

**Why 판정: ⏸ BLOCKED** — A-1이 이 목적 전체의 토대다. 영상과 모델이 다른 건물이면 이번 세션의 정합·스케일 작업 전부가 물리적 의미가 없고(A-2·A-3 연쇄 붕괴), 성공지표 검증도 불가능하다. **사용자 확인 필요.**

---

## 목표 B: 반복객체 현장에서 앵커 기준점 확보

| 항목 | 내용 | 출처 |
|---|---|---|
| 문제 정의 | 동일 배관/기둥이 반복되는 현장에서 영상-only 전역 위치가 다중해 | [문서:docs/scan-to-model-improvement-prd.md:35 "PXX 순환수배관 264개 반복"], [코드:realtime/server.py:1675 `_model_repetition_risk`] |
| 진짜 문제? | 실측 2회 CEILING 판정 — 벽앵커 단일 gap(0.878) ↔ 모델 폭 후보 5개(1.97/2.14/3.13/3.29/5.76) 원리적 미결정 | [사용자 세션 실측], [코드:scan2bim/wall_anchor.py:116 `_choose_scale`] |
| 왜 지금 | 🔴 목표 A의 차단 요인 — 정합 없이는 실적 판정 불가 | [문서:docs/scan2bim-auto-progress-prd.md:4 FR-2.4] |
| 안 만들면 | 매 업로드 수동 힌트 필요(현재 `--horizontal-scale-override` 상태) | [코드:tools/build_coplay.py `--horizontal-scale-override`] |
| 성공 기준 | 모호 자동확정 0%, 정합 성공률 ≥90% | [문서:docs/scan2bim-auto-progress-prd.md:9] |

### 앵커 후보 실측 검증 — **모델에 실제로 있는 것만**

| 후보 | 모델 존재? | 현장 존재? | 판정 |
|---|---|---|---|
| **텍스트·사인**("iaan LOUNGE") | ❌ **없음** — `attr` 365개 전부 Revit 파라미터(Category/Family Name/System Name/Diameter). `revitInfo`={} , `strattr`=0개. 실이름·사인 0건 [코드:models/Gasan_7F/*.dtdx 디코드] | ✅ | ❌ **매핑 불가** — 현장 전용 특징. PRD도 비목표로 명시 [문서:docs/scan2bim-auto-progress-prd.md:5 "OCR/텍스트 인식 — 텍스트 없는 현장 가정"] |
| **계통 색상**(빨강 소화배관) | ✅ material.diffuse=[1,0,0] | ✅ | 🟡 유효하나 **반복 해소 못 함**(빨강 배관이 다수) |
| **직경 + connector 토폴로지** | ✅ `attr` Diameter="100.000 mm", connector 74~609개 | 🟡 점군에서 직경 추정 필요 | 🟢 **반복 해소 후보** — PRD FR-2.3이 이미 지목, 미구현 |
| **가구 배치**(Office_Desk·회의용 테이블2·Office_의자) | ✅ AXX에 존재 | 🟡 시공중엔 미설치 | 🔴 as-built 시점에 없음 → 실적 목적과 상충 |
| **기하 토폴로지**(문→코너→개방부 순서) | ✅ | ✅ | 🟢 **반복 해소 후보** — 개별 반복돼도 순서+간격 조합은 고유 |
| **마감 외형**(목재 클래딩 기둥) | ❌ 구조체만 모델링 | ✅ | ❌ LOD 불일치 |

**Why 판정: ✅ 당위성 확인** (단 목표 A에 종속 — A가 BLOCKED면 B도 착수 보류)
